server:
  port: 7071

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/fortune_index?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 10000
      validation-timeout: 5000
      idle-timeout: 600000
      max-lifetime: 1700000
      keepalive-time: 300000
  jpa:
    hibernate:
#      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  email:
    verification:
      base-url: http://117.52.84.99:7071
      success-fallback-url: http://117.52.84.99:3001
      password-reset-fallback-url: http://117.52.84.99:3001/password-reset
